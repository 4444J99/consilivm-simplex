## Plan

1. Tighten the social metadata in `src/index.html` so the share preview is explicit and internally consistent.
2. Make the booking prices explicit across all three cards instead of mixing price into body copy.
3. Pull the work-section metrics out into structured case-study chips so the numbers scan immediately.
4. Remove the remaining dead CSS selectors that no longer match the HTML.
5. Verify with lint and browser tests where the sandbox permits, then create one commit per task.
